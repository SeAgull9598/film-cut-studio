# 拉片软件（影片分析工具）· 设计方案

> 版本：v2 设计稿（基于 2026-07-31 多轮沟通沉淀，含 20:19 四项对标调整）
> 状态：**方案确认中，尚未开始制作**
> 定位：纯前端、本地优先的「导演视角 · 声画技术拉片」工具

---

## 一、产品定位

- **一句话**：把一部影片拆成可分析、可回放、可导出的镜头单元，用导演视角把"感觉好"翻译成"可复制的方法"。
- **目标用户**：做人物纪录片 / 访谈的内容创作者（直接服务"退伍军人采访视频"后期），以及任何想做声画技术拉片的人。
- **不是什么**：不是剧情拆解 / 人物小传工具，不替代剪辑软件（是迷你 NLE 时间线 + 多维表格分析层，不是成片工具）。

---

## 二、设计原则（硬约束）

1. **隐私优先 / 本地优先**：所有处理在浏览器内完成，不上传任何服务器。退伍军人采访素材敏感，这是产品卖点而非可选项。
2. **声画技术取向**：对标 摄影And1 / 小鱼陪拉片 / 拉片实验室——用画面+声音讲故事、塑造人物。不混入学派（博然/中队长偏文本、风过大泽偏主题、拉片放映室是基础教学）。
3. **不跌入"截图陷阱"**：截图是单帧，拉片至少一半价值在跨帧连续（长镜运镜、蒙太奇组合、声画对位）。分析单位升级为「点 / 区间 / 段落组」三档。
4. **时间线 > 截图**：用"虚拟片段"（存 in/out 时间码引用源视频）替代导出/重编码视频，保住画面+声音+运动，零画质损失，数据量≈0。
5. **拆分 + 合并 必须成对**：只有拆分没有合并，时间线会碎成看不出整体。两者均非破坏性（只改时间码）。
6. **AI 分析默认走隐私路径**：默认"导出素材包 → WorkBuddy 手动分析"，不默认调用云端。
7. **界面与快捷键对标达芬奇（DaVinci Resolve）**：专业三栏布局（媒体池 + 检视器 + 时间线 + 检查器）、暗色主题、NLE 式快捷键，降低专业用户学习成本。
8. **维度标注对标飞书多维表格（Bitable）**：每镜头一行记录、维度即字段，支持字段类型、多视图（表格/看板/画廊/表单）、筛选/分组/排序。
9. **文本 / 语音输入对标 Notion**：块（block）编辑器、"/" 命令菜单、富文本、语音块本地转写。
10. **兼顾横屏与竖屏视频**：同时适配 16:9（横屏）与 9:16（竖屏，短视频/手机拍摄），检视器自适应、时间线按时间映射与分辨率无关。

---

## 三、核心功能模块（完整闭环）

```
视频导入(不出机) → 自动切分(SBD本地) → 时间线(虚拟片段,达芬奇式) → 拆分/合并/解散组
→ 多维表格标注(飞书式) → 块编辑器记录(Notion式) → AI 报告(Markdown/HTML)
```

### 模块 1 · 视频导入（不出机）
- 点击 / 拖拽导入本地视频；仅以 ObjectURL 载入内存，不落盘、不上传。
- 纯前端单文件，双击 `index.html` 即用，无后端无依赖。
- **横竖屏适配**：导入后读取视频 `videoWidth/videoHeight` 判定比例（16:9 或 9:16），后续检视器与缩略图据此自适应，不影响时间轴逻辑。

### 模块 2 · 自动切分镜头（SBD）
- 对标达芬奇 **Scene Cut Detection** / 剪映 **智能镜头分割**，本质是**镜头边界检测（Shot Boundary Detection）**。
- 工作流：**自动初切 + 人工精修**——先按切点拆出每镜铺满时间线，用户再选 / 合 / 拆 / 删。
- 技术两档：
  - **传统**：相邻帧颜色/亮度直方图差分 + 自适应阈值 + 最短镜头时长过滤（浏览器抽帧 2–4fps，秒级，硬切准、渐变弱）。
  - **AI**：TransNet V2（ONNX/WASM，几 MB，硬切 + 淡入淡出/叠化都准，F1 77–96%）。
- 交互：**灵敏度滑块**（= 达芬奇紫色阈值条，调高少漏检 / 调低减误报）。
- **横竖屏适配**：抽帧缩放到统一尺寸后算直方图，比例无关，横竖屏同样有效。
- 注意区分：剪映"智能镜头分割"（切镜头）≠ "AI 切片 / 高光识别"（内容理解），本工具要前者。

### 模块 3 · 时间线（虚拟片段，达芬奇式布局 + 快捷键）
- **布局对标达芬奇**：专业暗色三栏——左上「媒体池/片段列表」、右上「检视器（Viewer）」、底部「时间线（Timeline）」、右侧「检查器（Inspector）/ 标注面板」。
- 源视频只存一份；每个镜头 / 组存 `in/out` 时间码引用区间，**不重编码、零画质损失、数据量≈0**。
- 视觉：单镜 = 色块；组 = 色带（含子色块）。点色块 → 主播放器 `seek` 到 in、播到 out 自动停，可暂停 / 拖动 / 循环。画面 + 声音 + 运动全可播。
- 时间线坐标以「时间（秒）」为基准，与视频分辨率/比例无关，**横竖屏共用同一时间轴**。
- **快捷键（对齐达芬奇 NLE 习惯）**：
  | 键 | 功能 |
  |---|---|
  | `I` / `O` | 设入点 / 出点 |
  | `Shift+I` / `Shift+O` | 跳到入点 / 出点 |
  | `Space` | 播放 / 暂停 |
  | `J` / `K` / `L` | 倒放 / 暂停 / 正放（连按 L 加速，连按 J 倒放加速）|
  | `←` / `→` | 上一帧 / 下一帧 |
  | `Shift+←` / `Shift+→` | 跳到上一个 / 下一个剪辑点 |
  | `B` | 刀片工具（点击切分）|
  | `Cmd/Ctrl+B` | 在播放头切分当前片段（拆分）|
  | `A` | 选择 / 箭头工具 |
  | `G` | 将选中片段编组（合并为段落组）|
  | `Cmd/Ctrl+Shift+G` | 解散组 |
  | `Del` / `Backspace` | 删除片段 |
  | `Cmd/Ctrl+C` / `V` | 复制 / 粘贴片段 |
- 本质 = 迷你 NLE 时间线。

### 模块 4 · 拆分 / 合并 / 解散组（非破坏性）
- **拆分（Split / razor）**：在切点把一段切两段，用于长镜内含多动作时分别标注（`B` 或 `Cmd+B`）。
- **合并（Merge / Group）**：相邻片段框成「段落组」，组上写组合分析；点组播放 = 虚拟顺序连播（依次 seek 各成员 in/out）。快捷键 `G`。
- **解散组（Ungroup）**：可逆，回到原件。快捷键 `Cmd/Ctrl+Shift+G`。
- 全部非破坏性（只改时间码，可随时撤销）。

### 模块 5 · 维度标注（飞书多维表格式）
每个镜头 = 一行记录，维度 = 字段。对标飞书 Bitable 的数据库体验：

- **字段类型**：
  - 单行文本 / 多行文本（分析、备注）
  - 单选（景别：远 / 全 / 中 / 近 / 特）
  - 多选（运镜：固 / 推 / 拉 / 摇 / 移 / 跟）
  - 附件（关键帧缩略图，可选）
  - 关联（关联到所属「段落组」记录）
  - 公式 / 自动（时长 = out − in）
- **维度字段（声画派）**：
  1. 景别递进（远/全/中/近/特，及递进关系）
  2. 运镜意图（固/推/拉/摇/移/跟 + 为什么这样动）
  3. 场面调度（机位、人物走位、前后景关系）
  4. 光色（冷暖、明暗、时间/心理暗示）
  5. 声音设计（同期声/音乐/音效/静默；声画关系：同步 / 对位 / 错位 / 留白）
  6. 剪辑点（为什么在这切；硬切 / 叠化 / 匹配剪辑）
  7. 七问分析（综合：这一镜让观众离人物更近还是更远）
- **多视图**：
  - 表格视图（默认，Excel 式逐行标注）
  - 看板视图（按景别 / 声音设计分组，拖拽分类）
  - 画廊视图（镜头缩略图卡片，竖屏按比例裁切）
  - 表单视图（快速录入）
- **筛选 / 分组 / 排序**：如"只看特写"、"按景别分组"、"按时长排序"。

### 模块 6 · 文字 / 语音记录（Notion 式块编辑器）
- 每个镜头 / 组的记录区是**块（block）编辑器**，对标 Notion：
  - 支持块类型：文本 / 标题 / 列表 / 引用 / Callout / 分隔线。
  - 输入 `/` 唤起**块类型命令菜单**（slash menu）。
  - 富文本行内：加粗、斜体、高亮、行内代码。
- **语音块（本地转写）**：
  - 插入"语音块" → 点击麦克风录音 → 本地转写（Web Speech API 实时 / Whisper.wasm 事后，均不出机）→ 转写文本成为该块内容，原始音频可附在块上回放。
  - 记录按当前时间码 / 镜头关联，边看边口述极顺手。
- 记录与多维表格的「七问分析」字段可双向引用：块里的长篇分析是论证，表格字段是结构化结论。

### 模块 7 · AI 报告
- **素材包汇总**：镜头维度（多维表格导出）+ 文字 + 语音转写（块内容）→ 结构化文本。
- **AI 三模式**：
  - ① 本地 WebLLM（全隐私，小模型弱）
  - ② 云端 LLM API（用户自带 key，出机有泄露风险，非默认）
  - ③ 导出记录给 **WorkBuddy 手动分析**（最现实、质量最高、默认推荐）
- **报告结构**：镜头语言总览 / 声音设计 / 人物塑造手法 / 节奏与情绪曲线 / 可复制清单（5–8 条可直接抄进分镜）。

### 模块 8 · 导出与保存
- 导出 CSV（表格）、复制 Markdown（拉片表）、导出素材包（给 AI）。
- 本地保存：工程自动存（视频引用 + 所有标注）；大视频用 IndexedDB 存 Blob。
- **横竖屏适配**：导出缩略图按原视频比例裁切，不拉伸变形。

---

## 四、数据结构（草稿）

```
Shot  { id, in, out, groupId?, 景别, 运镜, 场面调度, 光色, 声音, 剪辑点, 分析, blocks[] }
Group { id, name, memberShotIds[], 组合分析, blocks[] }
Block { type: 'text'|'heading'|'list'|'quote'|'callout'|'voice', content, transcript?, audioRef? }
Project{ videoRef, videoRatio: '16:9'|'9:16', shots[], groups[], meta }
```

---

## 五、拉片方法论（工具内置引导，非仅功能）

- **五层模型**（自下而上）：单帧精度 → 单镜拆解（画面+声音）→ 镜头组接 → 一场戏的声画结构 → 人物塑造 / 叙事意图。
- **四遍法**：第一遍整体感受（找情绪锚点）→ 第二遍时间码打点 → 第三遍逐镜拆解（填声画维度）→ 第四遍提炼规律（节奏曲线 / 景别递进 / 声音设计）。
- **声画七问**：景别角度 / 场面调度 / 运镜 / 光色 / 声音 / 剪辑点 / 效果（离人物更近还是更远）。
- **人物访谈三重点（以退伍军人采访为例，作为通用声画方法，不预置为模板）**：景别递进（渐进靠近）、声音设计（日常声替代一问一答）、凝视克制（长镜特写、少字幕少解说）。

---

## 六、技术栈

- **载体**：纯前端单文件 `index.html`，双击即用。
- **播放 / 抽帧**：HTML5 `<video>` + `<canvas>`；横竖屏按 `videoWidth/videoHeight` 自适应。
- **自动切分**：ffmpeg.wasm 或纯 JS 抽帧算直方图差分；进阶 TransNet V2（ONNX/WASM）。
- **语音转写**：Web Speech API（Chrome 实时）/ whisper.cpp WASM（通用、隐私）。
- **多维表格 / 块编辑器**：自研轻量组件（字段类型 + 视图切换 + 块模型），无重依赖。
- **AI（可选）**：WebLLM（本地）/ 云端 API（用户 key）。
- **保存**：localStorage（轻量）/ IndexedDB（视频 Blob 与大工程）。

---

## 七、实现路线图（分阶段，当前未启动）

- **v2-1 核心骨架**：视频导入（横竖屏适配）+ 达芬奇式三栏布局与快捷键 + 时间线虚拟片段 + 点块即播 + 拆分/合并/解散组 + 飞书式多维表格标注 + Notion 式块记录 + 导出。
- **v2-2 自动切分**：SBD（传统算法先行，AI 进阶）。
- **v2-3 记录层增强**：语音块本地转写 + 素材包导出 + WorkBuddy 报告。

---

## 八、已知限制与取舍

- 浏览器对本地大视频有内存上限（IndexedDB 缓解，而非彻底解决）。
- Web Speech API 跨浏览器支持不一；Whisper.wasm 转写较慢。
- 本地 WebLLM 报告质量有限，故默认走 WorkBuddy 手动分析。
- 自动切分对运动模糊 / 叠化 / 同色镜头存在误报漏报，必须人工精修（达芬奇同理，提供灵敏度滑块 + Prune）。
- 导出真实视频片段（MediaRecorder / ffmpeg.wasm）仅作"分享"用，非主存储。
- **达芬奇快捷键学习成本**：普通用户需适应 NLE 键位；提供快捷键速查面板降低门槛。
- **横竖屏**：竖屏在宽屏显示器上检视器两侧留黑边属正常 letterbox，不拉伸；画廊缩略图按比例裁切。
