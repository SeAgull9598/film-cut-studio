# 拉片软件 · 需求规格说明书（软件开发表）

> 版本：v1.0 最终确认版
> 日期：2026-07-31
> 状态：**需求冻结，待开发**
> 形态：纯前端单文件 Web 应用（双击 `index.html` 即用，无后端）
> 面向：开发者 / 验收 / 回顾对照

---

## 0. 对标基线（需求来源）

| 维度 | 对标对象 | 吸收要点 |
|---|---|---|
| 剪辑交互 / 快捷键 | **达芬奇 DaVinci Resolve** | 三栏暗色专业布局（媒体池 + 检视器 + 时间线 + 检查器）、In/Out、JKL、刀片切分、编组/解散组 |
| 维度标注 | **飞书多维表格 Bitable** | 每镜头一行 = 记录，维度 = 字段；字段类型 + 多视图（表格/看板/画廊/表单）+ 筛选分组排序 |
| 文本 / 语音输入 | **Notion** | 块（block）编辑器、`/` 命令菜单、富文本、语音块本地转写 |
| 分析取向 | 摄影And1 / 小鱼陪拉片 / 拉片实验室 | 导演视角·声画技术拉片，用画面+声音讲故事、塑造人物 |
| 自动切分 | 达芬奇 Scene Cut Detection / 剪映智能镜头分割 | 镜头边界检测（SBD），自动初切 + 人工精修 |

**已取消项**：退伍军人采访专属模板预设（仅保留为方法论示例，不预置）。

---

## 1. 产品概述

- **一句话**：把一部影片拆成可分析、可回放、可导出的镜头单元，用导演视角把"感觉好"翻译成"可复制的方法"。
- **目标用户**：做人物纪录片 / 访谈的内容创作者，以及任何想做声画技术拉片的人。
- **非目标**：不是剧情拆解工具、不是剪辑成片工具、不是人物小传工具。
- **形态约束**：纯前端、本地优先、素材不出机。

---

## 2. 设计原则（强制约束 = 验收红线）

| 编号 | 原则 | 说明 |
|---|---|---|
| P1 | 隐私 / 本地优先 | 所有处理在浏览器内完成，不上传任何服务器；这是卖点非可选项 |
| P2 | 声画技术取向 | 对标声画派账号，不混入学派 |
| P3 | 三档粒度 | 分析单位 = 点 / 区间 / 段落组，截图降级为可选附件 |
| P4 | 时间线 > 截图 | 用"虚拟片段"（in/out 时间码引用源视频），不重编码、零画质损失 |
| P5 | 拆分 + 合并成对 | 两者均非破坏性（只改时间码），缺一则不验收 |
| P6 | AI 走隐私路径 | 默认"导出素材包 → WorkBuddy 手动分析"，不默认调云端 |
| P7 | 界面 / 快捷键对标达芬奇 | 三栏暗色专业布局 + NLE 快捷键 |
| P8 | 标注对标飞书多维表格 | 字段类型 + 多视图 + 筛选分组排序 |
| P9 | 文本 / 语音对标 Notion | 块编辑器 + `/` 菜单 + 语音块本地转写 |
| P10 | 横竖屏兼顾 | 同时适配 16:9 与 9:16，检视器自适应 letterbox，时间线按时间映射与分辨率无关 |

---

## 3. 需求总表（核心追溯表）

> 优先级：P0=必须有（缺则不验收）｜P1=重要｜P2=增强
> 模块：M1 导入 / M2 自动切分 / M3 时间线 / M4 拆分合并 / M5 多维表格 / M6 块记录 / M7 AI 报告 / M8 导出保存 / M9 小白友好

| 需求ID | 模块 | 需求描述 | 对标 | 优先级 | 验收标准 |
|---|---|---|---|---|---|
| R-001 | M1 | 本地视频导入（点击+拖拽），仅 ObjectURL 载入内存，不落盘不上传 | — | P0 | 任意本地视频可导入并播放；无网络请求产生（DevTools Network 为空） |
| R-002 | M1 | 导入后读取 videoWidth/Height 判定 16:9 或 9:16 | P10 | P0 | 比例正确识别并在工程中记录，后续检视器/缩略图据此自适应 |
| R-003 | M2 | 自动切分镜头（SBD），结果铺满时间线 | 达芬奇/剪映 | P1 | 点按钮后时间线出现初切片段，硬切基本准确 |
| R-004 | M2 | 灵敏度滑块控制检测阈值（=达芬奇紫线） | 达芬奇 | P1 | 调高少漏检、调低减误报，阈值变化即时反映在切点数 |
| R-005 | M2 | 人工精修：选/合/拆/删初切结果 | 达芬奇 Prune | P1 | 可删误报、可调边界、可补漏切 |
| R-006 | M2 | 最短镜头时长过滤，压制运镜误报 | 通用SBD | P1 | 短于阈值的抖动不产生伪切点 |
| R-007 | M3 | 达芬奇式三栏暗色布局：媒体池+检视器+时间线+检查器 | 达芬奇 | P0 | 四区可见，暗色主题，可缩放/折叠 |
| R-008 | M3 | 虚拟片段：每镜头/组存 in/out，引用源视频，不重编码 | P4 | P0 | 数据量≈原视频引用，零重编码；导出工程不含视频本体 |
| R-009 | M3 | 点色块即播：seek 到 in、播到 out 自动停，可暂停/拖动/循环 | P4 | P0 | 点任意片段，画面+声音+运动完整播放到 out 停 |
| R-010 | M3 | 时间线按"时间(秒)"映射，横竖屏共用同一轴 | P10 | P0 | 16:9 与 9:16 视频时间线行为一致 |
| R-011 | M4 | 拆分（在切点切两段）非破坏性 | P5 | P0 | 拆分后两段 in/out 之和等于原段，原数据可还原 |
| R-012 | M4 | 合并（相邻片段成段落组）非破坏性 | P5 | P0 | 成组后点组播放=虚拟顺序连播 |
| R-013 | M4 | 解散组（Ungroup）可逆 | P5 | P0 | 解散后回到原片段，组合分析保留可再组 |
| R-014 | M5 | 多维表格：每镜头一行，维度=字段 | 飞书 | P0 | 表格视图逐行可编辑；新增镜头自动加行 |
| R-015 | M5 | 字段类型：单选/多选/文本/附件/关联/公式(时长) | 飞书 | P0 | 景别可选单选项；运镜可多选；时长=out−in 自动算 |
| R-016 | M5 | 声画派 7 维度字段（景别递进/运镜意图/场面调度/光色/声音设计/剪辑点/七问） | 声画派 | P0 | 7 字段齐全，字段说明含"看什么/怎么写"提示 |
| R-017 | M5 | 多视图：表格/看板/画廊/表单 | 飞书 | P1 | 四视图可切换；看板按景别分组；画廊显示缩略图(按比例) |
| R-018 | M5 | 筛选/分组/排序 | 飞书 | P1 | 如"只看特写""按景别分组""按时长排序"生效 |
| R-019 | M6 | 块编辑器：文本/标题/列表/引用/Callout/分隔线 | Notion | P0 | 每个镜头/组有块编辑区；`/` 唤命令菜单；富文本行内格式 |
| R-020 | M6 | 语音块：录音→本地转写→文本回填，原音频可回放 | Notion+本地 | P1 | 录音后文本出现，不出机；音频可点击回放 |
| R-021 | M6 | 记录按时间码/镜头关联 | P3 | P0 | 口述/文本自动归档到当前片段 |
| R-022 | M7 | 素材包汇总（维度+文字+转写）并导出 | P6 | P1 | 一键生成结构化文本包 |
| R-023 | M7 | AI 三模式，默认模式③（导出给 WorkBuddy） | P6 | P1 | 默认不调云端；模式①②③可切换说明 |
| R-024 | M8 | 导出 CSV / 复制 Markdown / 导出素材包 | 飞书 | P0 | 三类导出均生成且内容正确 |
| R-025 | M8 | 本地保存：工程自动存；大视频用 IndexedDB 存 Blob | P1 | P0 | 关页重开数据在；大视频可存 |
| R-026 | M9 | 引导提示卡：每维度配"看什么/怎么写" | 小白友好 | P1 | 悬停/聚焦字段显示示例话术（可援引声画派账号） |
| R-027 | M9 | 术语速查 + 示例镜头库 | 小白友好 | P2 | 点"推镜头"等弹出定义+经典片例 |
| R-028 | M9 | AI 初填维度：点镜头→AI 给建议，人改 | 小白友好 | P2 | 单镜可触发 AI 草稿填入字段，可编辑 |

---

## 4. 分模块详细需求

### 4.1 M1 视频导入（R-001~002）
- 支持格式：浏览器 `<video>` 可播格式（mp4/webm/mov 视浏览器而定）。
- 交互：点击选择 + 拖拽入框；导入后显示文件名、时长、分辨率、比例标签。
- 横竖屏：读 `videoWidth/videoHeight`，≤0.85 判为竖屏(9:16)，≥1.7 判为横屏(16:9)，中间记原始比例。

### 4.2 M2 自动切分（R-003~006）
- 算法档位：
  - 传统（先行）：相邻帧颜色/亮度直方图差分 + 自适应阈值 + 最短镜头过滤；浏览器抽帧 2–4fps，秒级。
  - AI（进阶，v2 后接）：TransNet V2（ONNX/WASM），含渐变转场。
- 灵敏度滑块：阈值高→少漏检多误报；阈值低→反。实时反映切点数。
- 精修：支持删误报点、拖动点边界、补漏切点。
- 注意：剪映"智能镜头分割"（切镜头）≠ "AI 切片/高光识别"（内容理解），本工具只要前者。

### 4.3 M3 时间线 / 检视器（R-007~010）
- 布局（达芬奇式）：
  - 左上：媒体池 / 片段列表
  - 右上：检视器（Viewer，播放画面，aspect-ratio 自适应 letterbox）
  - 底部：时间线（Timeline，按秒映射）
  - 右侧：检查器 / 标注面板（当前选中片段的维度 + 块记录）
- 虚拟片段：源视频单份；每个镜头/组存 `{in, out}`。
- 播放：点色块 → `video.currentTime = in; video.play()`；`timeupdate` 监听到 `out` 自动 `pause()`；支持循环 / 允许拖出区间。
- 视觉：单镜=色块；组=色带（含子色块）；时间线可缩放。

### 4.4 M4 拆分 / 合并 / 解散组（R-011~013）
- 拆分 `B`/`Cmd+B`：播放头处切开当前片段为两段，非破坏性。
- 合并 `G`：选中相邻片段成"段落组"，组上写组合分析；组播放=依次 seek 各成员 in/out 虚拟连播。
- 解散组 `Cmd+Shift+G`：可逆，回到原件。
- 全部支持撤销（Ctrl+Z）。

### 4.5 M5 多维表格标注（R-014~018）
- 每镜头一行；维度=字段（声画派 7 维）：
  1. 景别递进（单选：远/全/中/近/特 + 递进关系文本）
  2. 运镜意图（多选：固/推/拉/摇/移/跟 + 为什么）
  3. 场面调度（机位、人物走位、前后景关系）
  4. 光色（冷暖/明暗/时间或心理暗示）
  5. 声音设计（同期声/音乐/音效/静默；声画关系：同步/对位/错位/留白）
  6. 剪辑点（为什么在这切；硬切/叠化/匹配剪辑）
  7. 七问分析（综合：这镜让观众离人物更近还是更远）
- 字段类型：单选 / 多选 / 单行文本 / 多行文本 / 附件(关键帧缩略图,可选) / 关联(到组) / 公式(时长=out−in)。
- 视图：表格(默认) / 看板(按景别分组) / 画廊(缩略图按比例裁切) / 表单。
- 操作：筛选 / 分组 / 排序。

### 4.6 M6 块编辑器记录（R-019~021）
- 每个镜头/组有块编辑区，对标 Notion：
  - 块类型：文本/标题/列表/引用/Callout/分隔线。
  - `/` 命令菜单；富文本：加粗/斜体/高亮/行内代码。
- 语音块：插入→录音→本地转写（Web Speech API 实时 / Whisper.wasm 通用，均不出机）→ 文本回填，原音频可回放。
- 记录按当前时间码/镜头自动关联。
- 块长篇分析 与 表格字段 双向引用：块=论证，字段=结论。

### 4.7 M7 AI 报告（R-022~023）
- 素材包：维度(表格导出)+文字(块内容)+转写(语音)→结构化文本。
- 三模式：
  - ① 本地 WebLLM（全隐私，小模型弱）
  - ② 云端 LLM API（用户自带 key，出机有风险，非默认）
  - ③ 导出给 WorkBuddy 手动分析（默认推荐，质量最高）
- 报告结构：镜头语言总览 / 声音设计 / 人物塑造手法 / 节奏与情绪曲线 / 可复制清单(5–8 条)。

### 4.8 M8 导出与保存（R-024~025）
- 导出：CSV（表格）、复制 Markdown（拉片表）、导出素材包（给 AI）。
- 保存：工程自动存（视频引用+标注）；大视频 Blob 存 IndexedDB。
- 横竖屏：导出缩略图按原比例裁切，不拉伸。

---

## 5. 交互规格：达芬奇快捷键表

| 键 | 功能 |
|---|---|
| `I` / `O` | 设入点 / 出点 |
| `Shift+I` / `Shift+O` | 跳到入点 / 出点 |
| `Space` | 播放 / 暂停 |
| `J` / `K` / `L` | 倒放 / 暂停 / 正放（连按加速） |
| `←` / `→` | 上一帧 / 下一帧 |
| `Shift+←` / `Shift+→` | 跳上一个 / 下一个剪辑点 |
| `B` | 刀片工具（点击切分） |
| `Cmd/Ctrl+B` | 播放头处拆分当前片段 |
| `A` | 选择 / 箭头工具 |
| `G` | 选中片段编组（合并为段落组） |
| `Cmd/Ctrl+Shift+G` | 解散组 |
| `Del` / `Backspace` | 删除片段 |
| `Cmd/Ctrl+Z` | 撤销 |
| `Cmd/Ctrl+C` / `V` | 复制 / 粘贴片段 |

**速查面板**：普通用户无需记键，提供可唤起的快捷键速查浮层（达芬奇学习成本兜底）。

---

## 6. 非功能需求

| 类别 | 要求 |
|---|---|
| 隐私 | 全程本地；无外部网络请求（除用户主动导出到 WorkBuddy）；不收集任何数据 |
| 性能 | 自动切分秒级（传统档）；时间线色块渲染不卡顿（建议虚拟滚动/分片渲染） |
| 横竖屏 | 16:9 与 9:16 均完整可用；竖屏两侧 letterbox 不拉伸；画廊缩略图按比例裁切 |
| 兼容 | Chrome 优先（Web Speech/转写最全）；Firefox/Safari 降级可用（转写走 Whisper.wasm） |
| 数据 | 工程可导出/导入 JSON，便于备份与跨设备；大视频 Blob 走 IndexedDB |
| 可访问 | 暗色主题满足对比度；关键操作有可见按钮兜底（不只靠快捷键） |

---

## 7. 小白友好补丁（纳入验收，R-026~028）

为让零基础用户也能产出接近声画派账号的分析，需实现：
1. **引导提示卡**：每个维度字段旁有"看什么 / 怎么写"提示，示例话术可援引摄影And1/小鱼（如"推近=逼近情绪"）。
2. **术语速查 + 示例镜头库**：点术语弹出定义 + 经典片例。
3. **AI 初填维度**：点镜头 → AI 给初步维度建议，人改（把 AI 报告能力下沉到单镜）。

---

## 8. 数据模型

```
Shot  { id, in, out, groupId?, 景别, 运镜[], 场面调度, 光色, 声音, 剪辑点, 七问, blocks[] }
Group { id, name, memberShotIds[], 组合分析, blocks[] }
Block { type: 'text'|'heading'|'list'|'quote'|'callout'|'voice', content, transcript?, audioRef? }
Project{ videoRef, videoRatio:'16:9'|'9:16'|'other', shots[], groups[], meta{title,createdAt} }
```

---

## 9. 验收里程碑（路线图）

| 阶段 | 范围 | 对应需求 |
|---|---|---|
| **v2-1 核心骨架** | M1 导入(横竖屏) + M3 达芬奇三栏/快捷键 + 时间线虚拟片段 + M4 拆分/合并/解散 + M5 飞书式多维表格 + M6 Notion 式块记录 + M8 导出 | R-001~002,007~018,019,021,024~025 |
| **v2-2 自动切分** | M2 SBD（传统档先行，AI 进阶后接） | R-003~006 |
| **v2-3 记录层增强 + AI** | M6 语音块转写 + M7 素材包/AI 报告 + M9 小白友好补丁 | R-020,022~023,026~028 |

**冻结说明**：本表为开发唯一对照基线；新增需求须走变更确认，不得私自扩范围。

---

## 10. 已知风险与取舍

- 浏览器大视频内存上限（IndexedDB 缓解，非根治）。
- Web Speech API 跨浏览器不一；Whisper.wasm 转写较慢。
- 本地 WebLLM 报告质量有限 → 默认走 WorkBuddy 手动分析。
- 自动切分对运动模糊/叠化/同色镜头有误报漏报 → 必须人工精修（达芬奇同理，灵敏度滑块 + Prune）。
- 导出真实视频片段（MediaRecorder/ffmpeg.wasm）仅作"分享"用，非主存储。
- 达芬奇快捷键有学习成本 → 速查面板兜底。
